import { NumberdirectiveDirective } from './numberdirective.directive';

describe('NumberdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
